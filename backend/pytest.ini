[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as requiring real LLM API keys (deselect with -m "not integration")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
