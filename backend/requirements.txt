fastapi==0.111.1
uvicorn[standard]==0.29.0
pydantic-settings==2.3.4
pydantic==2.8.2
supabase==2.5.3
PyJWT==2.9.0               # Supabase JWT verification (FR-7.1)
qdrant-client==1.9.2
python-dotenv==1.0.1
httpx==0.27.0

# ── Document intake ──────────────────────────────────────────────────────────
python-multipart==0.0.9       # FastAPI file uploads
docling==2.31.0               # PDF/DOCX extraction (primary)
pypdf==4.3.1                  # lightweight PDF fallback + test fixture generation

# ── LLM structured output ────────────────────────────────────────────────────
instructor==1.3.4             # Instructor (structured LLM output via Pydantic)
openai==1.35.14               # Required by instructor; used for OpenRouter compat
groq==0.9.0                   # Groq client (secondary LLM)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                 # async test client for FastAPI (already above, fine)
